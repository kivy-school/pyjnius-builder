from typing import Any, ClassVar, overload

class EapAkaInfo:
    def getReauthId(self) -> list[int]: ...

    class Builder:
        def __init__(self) -> None: ...
        def setReauthId(self, arg0: list[int]) -> "Builder": ...
        def build(self) -> "EapAkaInfo": ...
